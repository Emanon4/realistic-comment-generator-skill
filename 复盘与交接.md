# 水评生成维护说明

当前执行契约以 `SKILL.md` 为准，表格布局由 `scripts/build_xlsx.py` 实现。外部原稿或已丢失的旧参考表都不是运行依赖；仓库内40条金样保留为旧格式回归样例。

## 当前工作流

每次生成和审读10条，合格后继续；表格每列默认20条。纯文本预览按用户要求逐行输出，不夹带解释。飞书文档输入在表格模式下输出xlsx附件，不默认新建飞书电子表格。指定现有表格需有实际目标和可用写入工具。

新导出JSON使用 `review_batch_size: 10` 复查完整生成小批，同时保留 `batch_size: 20` 的排版检查。缺省审核字段的旧JSON继续使用原检查逻辑。10—19条尾列有效，1—9条尾列的自动重排尚未实现。

## 保留的经验

早期曾出现全无标点、全带句号、长度过度集中等问题，因此保留形式分布门槛。但形式合格不等于自然：同一观点换词、固定句序和总结腔仍由Agent逐条审读。默认以中性和认可为主，少量观望可选，不强制反对或热梗配额。

`--check` 先检查再原子写表，失败不创建或覆盖目标。`--stats` 会打印统计并导出文件，不是只读命令。

## 验证

```bash
python3 -m unittest discover -s tests -v
python3 scripts/build_xlsx.py tests/fixtures/good-natural-20.json /tmp/comment-natural-20.xlsx --check
```

回归测试应覆盖单独10条、两个合格小批汇成20条，以及20条整体合格但其中一个10条小批不合格时被拦截。同时保留旧金样、输入结构和失败不覆盖测试。

## 能力边界

当前导出器不执行语义审核，也不直接调用飞书。飞书读取与附件发送由部署环境实际提供的工具完成；文字契约和本地测试不能证明某个飞书部署已端到端可用。全表跨批近似重复的硬拦截和任意尾数自动排版仍留待独立任务。

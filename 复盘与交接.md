# 「水评生成」skill · 复盘与交接文档

> 给下一个接手的 AI（或几个月后的自己）。读完这份你就知道：这个 skill 在干嘛、为什么这么设计、踩过哪些坑、还差什么。**改 skill 之前先读完本文，尤其是第 4 节「踩过的坑」——那是这个项目反复翻车的地方。**

---

## 0. 一句话

给一段抖音视频脚本 → 生成一批「像真人随手留的、参差不齐的」评论 → 落成对齐参考表的 xlsx（或飞书电子表格），供创作者在**自有账号、自有视频**的评论区做互动运营。**轻量工具，不做调研，脚本进、评论表出。**

---

## 1. 用户与背景

- 用户是内容创作者，做抖音/短视频，有一套完整的**「迷因方法论」手册**（选题调研、捕捉大众情绪、内容生产等），那是这个 skill 的「最高指导思想」。
- 用户团队在**飞书**里办公，最终要把这个 skill 装进一个**连接飞书的 AI Agent**：用户在飞书发一个飞书云文档（脚本）给 Agent，Agent 读文档 → 生成评论 → 直接在飞书里生成电子表格。这是**主力部署场景**，设计时必须照顾它（见第 3 节）。

### 关键源文件（下一个 AI 要回查时看这些）
| 用途 | 路径 | 状态 |
|---|---|---|
| 产品规格母本（角色/流程/安全边界/输出格式/示例） | 原 `~/Downloads/…comment-operations-master.md` | ⚠️ **已从原路径丢失**。其内容（角色/流程/安全边界/输出格式/示例）当初已全部内联进 `SKILL.md`，**现以 SKILL.md 为唯一权威**，母本不再是必需件 |
| **参考表金样**（输出 xlsx 要长这样） | 原 `~/Downloads/蒸汽电波-可灵-200条.xlsx` | ⚠️ **已丢失**。布局的唯一真相源现在是 `scripts/build_xlsx.py`；仓库内 `references/样例-金样-40条.xlsx` 是它生成的活样板，要看「长这样」跑脚本或打开它即可 |
| 迷因方法论手册（评论真实性的理论依据） | 本地外部资料，未随仓库分发 | 运行不依赖 |
| 选题调研手册（评论区鉴别术） | 本地外部资料，未随仓库分发 | 运行不依赖 |

---

## 2. 文件结构（本 skill 自身）

```
水评生成/
├─ SKILL.md                        ← 正本、自足。生成规范全部内联在这里
├─ references/
│  ├─ realism-and-types.md         ← 背景/详版。以 SKILL.md 为准，读不到它也不影响运行
│  └─ 样例-金样-40条.xlsx          ← 输出 xlsx 的活样板（build_xlsx.py 生成，替代已丢失的旧金样表）
├─ scripts/build_xlsx.py           ← 零依赖 xlsx 导出器 + --stats 参差自检 + --check 硬门槛
└─ 复盘与交接.md                    ← 本文件
```

**改动纪律**：SKILL.md 是唯一权威。references 只是背景，别让它和 SKILL.md 冲突。改了生成规范，两边都要同步（或至少别矛盾）。

---

## 3. 核心设计决策（为什么这么做）

1. **安全边界（来自 master 文档，不可删）**：只服务创作者自有账号自有视频的互动运营。不批量刷他人评论区、不冒充消费者证言、不虚假疗效收益、不造谣、不攻击真人。还加了「工具诚实」硬规则：工具没绑定/报错就降级，绝不编造正文、绝不谎报"已生成表格"。
2. **输出格式对齐「蒸汽电波」参考表**：左侧元数据块（账号 A1 / 发布日期 A2 / 下单日期 A3 / 视频链接 A4 合并 A4:A6 / 总数 A7）+ 批次评论从 D 列起、每批次一列、每列 20 条（行 2–21）、每 5 批一组组间空一列（D–H 空I J–N）。
3. **条数：批次铺量、可配**。默认 200 条 = 10 批次 × 20。（master 文档写的是「单视频≤20」，但用户明确选了「批次铺量」对齐参考表——以用户选择为准。）
4. **视频链接始终留空**：Agent 拿不到用户「即将发布」的视频链接，用户发布后自己补。账号、日期也是用户可编辑字段，交付时要提醒用户补这三样。
5. **SKILL.md 必须自足**：主力场景（飞书 Agent）**没有文件系统、跑不了 Python、不会自动加载子文件**。所以七类骨架、配比、语料、自检、表结构、JSON 字段**全部内联在 SKILL.md 正文**，不能只丢在 references/ 或脚本注释里（早期版本犯过这个错，红队判飞书 Agent `ok:false`）。
6. **三种落表法**：A 飞书电子表格（飞书 Agent 首选，依赖平台绑定写表插件，不支持合并单元格时降级为单格写）＞ B 本地 xlsx（有 Python 时跑 build_xlsx.py）＞ C 兜底贴 Markdown 表。

---

## 4. 踩过的坑 ⚠️（这个项目最重要的教训，别重蹈覆辙）

### 坑：「参差不齐」是**多轴方差**，不是某个固定比例/平均值

这是整个项目**翻车三次**的地方。经过如下：

1. **v1 · 过拟合**：从「蒸汽电波」那张表实测出「100% 不加标点」，当成通用铁律写进 skill → 生成结果**清一色不加标点**，中长评还用**空格代替标点**（「沉默的用户撑起了百度 发声的用户重塑了口碑」）。用户一眼判假。
2. **v2 · 矫枉过正**：改成「中长评多数正常用标点」→ 模型又滑到**另一个极端**：几乎每条都是 15–25 字完整句、都以 `。/？` 收尾。长度一个模子、收尾一个模子。**这仍然是"统一"，只是换了种统一。** 用户再次判假。
3. **v3 · 三轴配额**：把生成核心从「描述平均值」改成**「长度 × 收尾标点 × 句式结构」三轴硬配额 + 正反例对照 + 自动检测器**。然而检测器只限制无标点不能太多，没有限制不能太少，导致 0 条无标点也可能通过。
4. **v4 · 双向标点门槛（当前版）**：每批无收尾标点目标改为 8–10 条，并在 `--check` 中硬卡 35%–60%。中长评论可以保留内部逗号后直接以文字收尾，避免把「自然」误解成删掉所有标点。

**教训（务必记住）：**
- **用形容词/平均值描述（"多数""参差"）会让 LLM 朝那个方向一边倒。** 要逼出方差，必须给**硬配额**（每 20 条里各档各多少）+ **正反例**（贴一段"太统一"的反例和一段"真参差"的正例，这是教 LLM 最狠的杠杆）+ **可执行的自动检测**。
- **真参差 = 极端之间的落差**：2 字爆点（离谱/典/绝）和 30 字长评并排、不加标点的和句号收尾的并排、单词碎片和完整句并排。不是"都往中间凑"。
- **标点向 master 文档看齐**：master 文档自己的示例评论**用正常标点**（「早起不难，难的是长期把自己的节奏稳定下来。」）。所以不是"不加标点"，是"有的加有的不加，混着来"。
- **空格代替标点 = 最假的机器痕迹**，明令禁止（英文/数字间空格如「Scaling Law」正常保留）。

---

## 5. 质量校验怎么用

导出时加 `--stats`，自动体检参差度；加 `--check` 则把体检变成**硬门槛**（不达标退出码 2）：
```bash
python3 scripts/build_xlsx.py 输入.json 输出.xlsx --stats          # 只打印
python3 scripts/build_xlsx.py 输入.json 输出.xlsx --check          # 不达标 → exit 2，可驱动「自动重写该批」
```
会打印**整池鸟瞰 + 逐批的（长短分布 / 收尾方式 / 句式 run-on 碎片 / 空格分句）**，并在下列情况判该批不达标：
- 无标点 < 35% → 每条都工整收口；无标点 > 60% → 又回到全不加标点
- 除无标点外，某种单一收尾（句号等）> 40% → 收尾太统一
- 中 15–25 字 > 45% → 长度都挤中间
- 极短 ≤6 字 < 15% → 两字爆点太少
- 中文字之间夹空格 → 空格分句机器痕迹
- 超长 40+ 字 > 0 → 抖音评论区几乎没有
- ≥3 条 16 字以上的较长评论却 0 条 run-on（长句全在规矩断句）→ 句式偏统一
- 末列不满 batch_size / 空表 → 条数缺角（`--check` 也会拦）

> **两个关键设计（都是踩坑后补的）**：
> 1. **逐批判定，不看整池平均**：三轴配额是「每 20 条一批」定义的，所以 `--stats`/`--check` 按 `batch_size` 切片、**逐批**跑自检并报出失败批次序号（如「第3批 ✗」）。早期只统计扁平整池——一个 100% 统一的烂批会被其余批平均稀释、整池判合格，「不达标自动重写该批」就落不了地。整池鸟瞰只留作参考、不参与判定。
> 2. **阈值用精确百分比**：早期用向下取整 `v*100//n`，会把 40.9% 显示成 40% 而漏报紧贴阈值的不达标——已改成浮点比较。

**判达标的直觉**：长度五档都要有、别挤中间；无标点占 35%–60%，其余收尾别让一种压倒；空格分句必须 0；较长评论里要有一句到底的 run-on。（注：其中「收尾要有六七种」「五档都非空」是**人工肉眼**的更高追求，`--check` 并不强制这两条——它只硬卡上面那串具体阈值；所以 `--check` 通过 ≠ 一定够参差，仍要看 `--stats` 分布。）

### 当前版实测（v3，百度「AI 黄埔军校」主题；即仓库内 `references/样例-金样-40条.xlsx`，40 条=2 批、`--check` 逐批通过）
- 长短：极短 30% / 短 28% / 中 25% / 长 18% / 超长 **0%**（五档铺满，超长压到 0）
- 收尾：无标点 58% · 句号 22% · 问号 8% · 感叹 5% · 省略 5% · 波浪 2%（六种，无一超 40%，无标点未超 70%）
- 句式：run-on 4 · 碎片 11；空格分句 0 ✓

---

## 6. 待办 / 未决 / 下一个 AI 可以接着做的

1. ~~**名字存疑**~~ **已定**：确认是「**水评**」（水军评论），旧「水平生成」是同音笔误。项目目录、SKILL.md（`name`/标题/触发词）、references、本文档均已统一为「水评生成」；触发词同时保留「水平生成」作旧别名兜底。
2. **飞书部署未实测**：目前所有验证是在 Claude Code 里**模拟 + 红队**做的，还没在真实飞书/Coze 环境跑过。真上飞书需平台侧给 Agent 绑定两样能力：① 读飞书云文档 ② 新建/写飞书电子表格。
3. **飞书电子表格落法(A)的写入契约是通用描述**，具体调哪个插件/API、文件夹 token 从哪来，因平台而异，没固化——真部署时按平台补实。
4. **配比阈值可再调**：三轴配额和 --stats 的阈值（无标点 35%–60%、其他收尾 40%、中评 45%、极短 15%）是经验值，真实评论区跑多了可以校准。
5. ~~把参差自检做成硬门槛~~ **已做**：`--check` 不达标即退出码 2、**并按 batch_size 逐批检测、报出失败批次序号**，配合生成循环可实现「不达标自动重写该批」；`--stats` 也已补上句式(run-on/碎片)第三轴、且做了输入清洗（None/空条跳过、非串转串）。**仍可选**：给 build_xlsx.py 加飞书 Sheets API 直写（现在只出本地 xlsx）。
6. **金样表已丢**：旧 `蒸汽电波-可灵-200条.xlsx` 与产品母本已不在原路径。布局真相源现在是 build_xlsx.py；仓库内 `references/样例-金样-40条.xlsx` 是新生成的活样板。若日后找回旧金样，核对一下列宽/合并等细节即可。

---

## 7. 给下一个 AI 的一句话

> 改 skill 就改仓库根目录里的 SKILL.md / references / build_xlsx.py；改完**务必用 `--stats`/`--check` 自查参差**。**别把「参差」理解成某个固定比例——它的本质是「别让任何单一模式（某个长度档、某种收尾、某种句式）占多数」。** 想改真实感规则前，先重读第 4 节，别再掉进「用平均值描述→模型一边倒」的坑。

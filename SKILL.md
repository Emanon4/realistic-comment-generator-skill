---
name: realistic-comment-generator
description: |
  水评生成：给一段抖音/视频号视频脚本（口播稿、简介、分镜文案），直接生成一批
  自然口语的评论候选，供创作者在自有账号、自有视频中由真实运营人员审核、改写后使用。
  真实性靠内容锚定和表达差异——长短参差、至少约三分之一不带收尾标点、口吻与类型混杂。
  输入可以是脚本文字，也可以是飞书云文档（有读取能力就直接读正文）；产出对齐参考表的表格——
  可导出本地 .xlsx，或在飞书 Agent 环境直接生成飞书电子表格。表结构：账号/发布日期/下单日期/视频链接/总数 + 按批次分列、每列20条。
  不做选题调研、不解析真实评论区——它是轻量生成工具，脚本进、评论表出。
  触发词：「水评生成」「水评」「水平生成」「生成评论」「给这条脚本写评论」「配评论」「评论区话术」
  「按这个脚本生成N条评论」。用户粘贴脚本/口播稿+说「生成评论」也触发。
  边界：只服务创作者自有账号自有视频的互动草稿；不自动批量发布、不虚构评论者身份或
  消费经历、不冒充消费者证言、不虚假疗效收益、不造谣、不攻击具体真人或群体、不做平台刷量。
  不触发：写脚本/改文案本身（上游编导活）、给成片提修改意见（那是审片）、真实评论区调研
  （那是选题调研，另有手册）。
---

# 水评生成 · 真实评论生成器

> **给一段脚本，生成一批锚定内容、表达参差的评论草稿，落成对齐参考表的表格。**
> 真实感不来自单条多妙，来自一堆凑在一起的**参差**——长短不一、标点不齐、口吻各异、类型混杂。整齐 = 假。

> **本文件自足**：生成所需的全部规范（七类骨架、配比、语料、反面清单、自检、表结构、JSON 字段）都**内联在正文**里，不依赖任何外部文件。`references/realism-and-types.md` 只是更详的背景版，读不到它也不影响运行。

## 运行环境（按「实际可用工具」择路，不按部署类型假定能力）

同一份 skill 会跑在不同环境。**不要假设自己拥有某种能力，先探测再用**：

| 环境 | 读脚本 | 落表 |
|---|---|---|
| **飞书里的 AI Agent（主力场景）** | 用飞书云文档读取能力读正文 | **优先**新建飞书电子表格（落法 A） |
| **Coze/扣子 等插件 bot** | 若绑定了飞书读取插件则用，否则请用户粘贴 | 有写表插件走 A，否则兜底贴表（C） |
| **Claude Code / 有 Python+浏览器** | 浏览器工具或 WebFetch（私有飞书文档读不到，见第0步） | 跑 `scripts/build_xlsx.py` 出本地 .xlsx（落法 B） |

**择路顺序**：先确认能读到脚本正文（读不到就请用户粘贴），再按**实际绑定的能力**挑落表方式——飞书电子表格 > 本地 .xlsx > 兜底贴表。

## 角色定位与安全边界（宪法，所有步骤服从）

生成的评论**仅供创作者在自有账号、自有视频中，由真实人员审核并以本人身份使用**。**不用于**：在他人作品下批量发布、操纵评论区、平台刷评刷量、虚构多名评论者身份、冒充真实消费者做误导宣传、规避平台规则自动灌水。

生成内容不得包含：虚假消费者证言、编造的具体购买/使用/消费经历、虚假疗效或收益承诺、攻击辱骂具体真人或特定群体、造谣、诱导规避平台审核、可复制到无关视频的万能灌水评论。用户若明确要求上述违规内容，拒绝该部分，改为提供合规的自有账号互动话术。

**工具诚实（硬规则）**：任何「读文档 / 建表 / 存文件」动作**必须先真实调用对应工具**。若该工具没绑定、或调用报错——**绝不编造读到的正文、绝不谎报「已生成表格」**。直接如实告诉用户，并降级：读不到 → 请用户粘贴正文；建不了表 → 退到兜底贴表（C）。宁可说「我这边建不了飞书表，先把表贴给你复制」，也不许假装建好了。

评论必须**围绕视频内容本身**、锚定脚本里的具体点位，自然口语。

---

## 工作流

### 第 0 步 · 输入识别
| 输入 | 处理 |
|---|---|
| **抖音链接**（含 `v.douyin.com`/`douyin.com`，或"抖音链接"四字） | 不解析、不抓取、不猜内容。固定回复：`抖音链接暂时不支持自动解析，请直接粘贴视频口播稿或视频简介，我来帮你生成评论。` 等用户粘脚本。 |
| **飞书云文档 / 链接**（`feishu.cn`/`larksuite`/`feishu.net`，或飞书里直接发来的文档） | 用**当前环境实际绑定的飞书读取能力**读正文。飞书 Agent → 原生云文档读取。**Claude Code / WebFetch → 读不到需登录的私有飞书文档**（只会拿到登录跳转页或空壳），遇私有链接**直接请用户粘贴正文**，仅当公开可访问、或用户 Chrome 已登录飞书并授权时才试浏览器工具。读到 → 正文当脚本进第 1 步；读不到 → 请粘贴，**不硬猜、不编造**。 |
| **脚本文字**（口播稿/简介/分镜/标题+正文） | 直接进第 1 步。 |
| **批量文件**（Excel/CSV/多段结构化文本，每段一个视频） | 逐个视频各自生成，对每个视频循环第 1 步。 |

### 第 1 步 · 解析脚本（抓"评论会长在哪"）
读一遍脚本，标出评论会围着打转的点位（后面每条评论都要锚定其中之一）：
- **情绪爆点**：最戳人、最反转、最出乎意料的地方
- **金句**：可被复述的核心判断（复述金句型评论的原料）
- **画面/细节**：具体动作、表情、数字、名字（具体细节承载真实感）
- **互动口**：脚本刻意留的表达入口（提问、留白、选择、报身份）
- **圈层与话题**：视频落在哪个人群/话题（决定措辞门槛和讨论角度）；没有账号定位或真实受众资料时不虚构方言、职业和生活经历

### 第 2 步 · 配置（能默认就默认，别过度打断）
- **条数 / 批次**：默认先生成 **20 条校准批**。用户确认账号口吻后，再按要求扩写；需要 200 条时生成 10 批次 × 每批 20 条。批次大小默认 20，只接受 10–100。
- **账号名**（→B1）：默认填用户给的账号；没给、或可能是别的号发的 → 占位 `待填`，交付时提醒确认。
- **发布日期 / 下单日期**：默认留空，用户按实际填。
- **视频链接**：**默认始终留空**——你拿不到用户「即将发布」的那条视频链接，用户发布后自己补。脚本正文里就算带了别的链接，也别当视频链接硬塞（除非用户明确说「就用这条」）。
- **账号定位 / 风格要求**（可选）：有账号定位时记录其版本并优先执行；用户提供的真实受众口吻可以参考，但不得凭空虚构宝妈、学生、从业者等身份经历。没提供时使用通用基线，并把首批 20 条作为校准样本。

一次问清即可，不为每个视频反复弹配置。

### 第 3 步 · 生成（**按配额构造 + 逐批 20 条**，规范全内联在下方）

不要「先随便写再回头数」。**逐批生成、每批按配额出**，天然达标，也避免一次吐 200 条越写越雷同/被截断：

**核心目标：把「长度 × 收尾标点 × 句式结构」三个轴的方差同时拉满。任何单一模式都不能占多数。** "参差不齐"不是某个平均值，是**极端之间的落差**——同一批里，2 个字的爆点和 30 个字的长评并排、不加标点的和句号收尾的并排、单词碎片和完整句子并排。手机评论区里很多人打完就发，不会给每条完整句补句号；**每批至少约三分之一必须以汉字、字母或数字直接收尾**，不能只靠「偶尔一个没标点」装自然。

**先看正反例（这是判断参差够不够的标尺）：**

❌ **假·太统一**（就是要避免的毛病——每条都是 15–25 字完整句、都以 。/？收尾，长度一个模子、收尾一个模子）：
> 苹果Intelligence选百度搜索，说是技术认证也挺微妙。
> 一线城市只有10.4%，难怪我身边没一个人用百度。
> 吐槽的人和使用的人不是同一群，这逻辑一下就通了。
> 达里奥2014在百度实验室，现在Anthropic估值多少了？

✅ **真·参差**（长度 1→28 字乱铺，收尾有无标点/。/！/？？/……/~/哈哈哈，句式有单词/碎片/完整句/一句到底）：
> 离谱
> 家里长辈就是这样
> 破防了！
> 高血压吃什么、社保怎么交，这不就是很多长辈每天搜的吗？
> 典
> 那些人当年在百度没声量，出来全成大佬了。
> 就……惯性这玩意太可怕
> 达里奥居然待过百度实验室？？
> 提醒八百遍换个AI，家里长辈还是会打开百度
> 苹果都选百度搜，说明技术底子还在，就是产品拉了。
> 绝
> 百度成AI黄埔军校了哈哈哈哈
> 不是百度没人用，是我们的百度没人用~

**三轴配额（每 20 条一批，都要拉开，别让任何一档/一种占多数）：**

| 轴 | 要求（每 20 条） |
|---|---|
| **长度**（别都挤在中间） | 极短 2–6 字 **5–6 条**、短 7–14 字 **4–5 条**、中 15–25 字 **6–7 条**、长 26–40 字 **3–4 条**、超长 40+ **0 条**。**每批恒为 20 条（硬）**；各档条数是软引导，在给的区间内取值、彼此凑成正好 20（别把各档上限直接相加，那会是 22）。长度要从 2 字一路铺到 35+ 字。 |
| **收尾标点**（别写成每条都收得很完整） | 不加任何收尾标点 **8–10 条**、句号。**3–4 条**、问号？**2–3 条**、感叹号！**1–2 条**、省略号……**1 条**、~ 或 emoji **0–1 条**、逗号断句但结尾不收 **1 条**。在区间内组合成正好 20 条；批次不是 20 条时按比例缩放。**无标点不得少于 35%，也不得超过 60%；连续 4 条同种收尾＝失败。** |
| **句式结构**（别都完整句） | 单词/短语碎片（典/绝/绷不住了）**3–4 条**、感叹或情绪短句（破防了/我真的会谢）**2–3 条**、一句到底不断句的 run-on **2–3 条**、半句话/欲言又止（就……很离谱）**1–2 条**、其余正常完整句。 |

**标点按人设和语气决定，不要先写一批工整句子再机械删句号。** 极短、口语、接梗、报到式评论优先不加收尾标点；中长评论可以保留内部逗号，却直接以最后一个字收尾，例如「苹果都选百度搜，说明技术底子还在」。追问也允许写成「这个去哪申请」。句号只留给少量讲道理或明确断言的评论。

**❌ 严禁用空格代替标点**：「沉默的用户撑起了百度 发声的用户重塑了口碑」这种拿空格分句是**最假的机器痕迹**——该逗号就逗号、该句号就句号、不断句就一句到底，绝不用空格顶替。（英文/数字间空格如「Scaling Law」「DeepSpeech 2」正常保留。）

**七类评论骨架（一批里尽量覆盖、比例不均、顺序打乱、允许不同意见）：** 每条锚定脚本里的**具体点位**，不许泛评全片。
1. **惊叹/爆点型**（多为极短）：`离谱`｜`绝了`｜`恐怖如斯`
2. **共鸣/场景型**（描述脚本能够支持的日常场景；除非用户明确提供真实背景，否则不写「我是宝妈」「我也是干这行」等身份经历）：`家里长辈确实更习惯搜索框`｜`每天先折腾工具再开工太像很多人的状态`
3. **不同意见/反驳型**（形成讨论但不攻击人）：`这个结论也不能一概而论`｜`老版本可能不是这样`
4. **追问/畅想型**：`那10.4%的人出来现个身`｜`求个内测地址`（问句自然带问号，也可不带）
5. **复述金句型**（用自己的话重述核心判断）：`两代人不共享一个互联网，这句有点东西`
6. **玩梗/二创型**：`横店连夜拆除`｜`百度成AI黄埔军校了哈哈哈`
7. **行业黑话/专业视角型**：`DeepSpeech 2真是百度做的`｜`Scaling Law早期证据也是百度美研`

**语料（克制，别堆梗、别用过时梗）**：情绪弹（离谱/离大谱/绝了/绝子/麻了/破防/红温/瑞思拜/逆天/跪了/震撼/哈人/绷不住了/典/绝）、代入（我也是/太真实了/说的就是我/我们公司也）、反差（没想到/第一次见/卧槽真的假的）、追问（同问/蹲/+1/求链接）。含梗评论约占 **25%**，其余大白话。

**反面清单（出现即"假"，逐批自查）**：❌ **每条都同长**（都挤在 15–25 字）❌ **每条都同收尾**（都句号，或都问号，或都不加）❌ 每条都是完整句、同句式开头 ❌ 所有评论都无条件赞同、没有保留意见 ❌ **用空格分句** ❌ 机械替换关键词 ❌ 泛评全片的万能水评（"太厉害了""做得真好"）❌ 堆过时梗/书面语/新闻腔 ❌ 冒充消费者证言、编造购买经历。

**多种表达角度**：同一批要有不同关注点和语气，但不得借此虚构多个具体身份或消费经历。

### 第 4 步 · 逐批自检（这一批过了再写下一批）
1. **三轴都拉开了吗**（最关键）：①**长度**——从 2 字爆点铺到 35+ 字长评，没都挤在 15–25 字？②**收尾**——逐条数过无标点了吗，每 20 条至少 7 条、目标 8–10 条，并混有。/！/？/……，没连着 4 条同种收尾？③**句式**——有单词碎片、有 run-on、有完整句，不是清一色完整句？
2. **没用空格分句吧？**（中文字之间夹空格＝机器痕迹）
3. **类型混、有不同意见**：顺序打乱了吗？不同意见是否针对观点而非攻击人？
4. **条条锚定具体点位**，非泛夸？
5. **表达角度不同、无雷同句式、无机械换词**？
6. **无越界**（虚假证言/攻击真人/造谣）？
不合格当场重写这一批不达标的条目。全部批次集齐后，**核对总条数正好 = 要求数、且能被批次大小整除**（末列满 20），再落表。

### 第 5 步 · 落表并交付（按环境择一：飞书表 A > 本地 xlsx B > 贴表 C）

三种落法**用同一套表结构**（见下方「输出表格格式」，行号务必照抄）。

**A · 飞书电子表格**（飞书 Agent 环境首选，**依赖已绑定「可写飞书电子表格」的插件，非默认能力**）
1. **前提自检**：确认已绑定可写飞书电子表格的能力、且有目标文件夹。**没有就别试 A，直接降级到 C**（别假装建好了，见宪法·工具诚实）。
2. 新建电子表格，表名 `{账号}-{话题关键词}-{条数}条`。
3. 按「输出表格格式」的行号写左侧元数据块 → 从 D 列起逐列写 20 条。
4. **合并单元格降级**：很多飞书写表能力不支持合并单元格。**不支持就别合并**——只在 A4/B4 写「视频链接」及内容，A5:A6、B5:B6 留空即可，不影响使用。

**B · 本地 .xlsx**（**仅限有本地文件系统 + 能跑 python 的环境，如 Claude Code**；Coze/纯插件 bot 用不了，跳到 A 或 C）
1. 把结果写成 JSON（结构见下）到临时目录（如 scratchpad）。
2. 跑 `python3 <本skill目录>/scripts/build_xlsx.py <输入.json> <输出.xlsx> --check`（零依赖；`<本skill目录>` 替换成 SKILL.md 所在的绝对路径）。脚本会先校验再原子写表；不达标时退出码 2，**不会创建或覆盖目标文件**。`--stats` 可单独打印分布。
3. 存到用户指定文件夹（默认 `~/Documents/`，别默认丢 `~/Downloads/`——本项目的母本和旧金样就是在 Downloads 弄丢的），文件名 `{账号}-{话题关键词}-{条数}条.xlsx`。

**落表 JSON 结构（A、B、C 都按这套字段组织数据）：**
```json
{
  "account": "账号名",
  "publish_date": "",
  "order_date": "",
  "video_link": "",
  "batch_size": 20,
  "comments": ["评论1", "评论2", "……共 N 条，脚本按 batch_size 自动切列"]
}
```

**C · 兜底·贴表**（既不能建飞书表也没 Python 时）：直接在对话里输出文本/Markdown 表格供用户复制。

**交付话术（必带，提醒用户补 3 处可编辑字段）：**
> 表格已生成（共 N 条 / M 批次）。发布前请你补 3 处：
> ① **视频链接**——我拿不到你即将发布的视频链接，请发布后自己贴上；
> ② **发布日期 / 下单日期**——按实际改；
> ③ **账号**——确认是哪个号发的（可能不是默认那个）。
>
> 附前 6–10 条预览让你先感受参差：……

---

## 输出表格格式（对齐参考表，三种落法共用；行号照抄）

**左侧元数据块（逐格写，行号固定）：**
| 单元格 | 内容 |
|---|---|
| A1 / B1 | 账号 / `{账号名}` |
| A2 / B2 | 发布日期 / 留空 |
| A3 / B3 | 下单日期 / 留空 |
| A4 / B4 | 视频链接 / **留空**（合并 A4:A6、B4:B6；不支持合并就单格写、A5:A6/B5:B6 留空） |
| A7 / B7 | 总数 / `{实际条数}` |

**批次评论块**：从 **D 列**起，每批次占一列——**每列第 1 行写批次表头**（第一批次…），**第 2–21 行写 20 条评论**。每 5 批次一组、组间空一列（复刻参考表 D–H · 空I · J–N）——**组间空列只是观感，落飞书表或手工排做不到就让批次列连续排，不强求**；批次表头「第一批次…第五批次」每组内循环。评论本身不加序号、不加引号、不写解释。

## 批量模式
文件含多个视频时，逐个视频各自生成一张表；每个视频独立命名，不改变视频顺序。同一套风格与条数应用于所有视频。

## 生成前最终自检
1. 有脚本了吗（不是链接、不是空、不是编造的）？　2. 条数/批次定了吗？　3. 单条还是批量？
4. **三轴拉开了吗**：长度从 2 字到 35+（长26–40字至少约10%）、无收尾标点占 35%–60%、至少四种收尾、连续同收尾不超过三条、句式混（碎片+run-on+完整句）？
5. **没用空格分句**、没堆过时梗吧？　6. 类型混、有针对观点的不同意见吗？
7. 条条锚定具体点位、万能泛评不超过20%吗？　8. 表达角度不同、无雷同句式、无机械换词吗？
9. **总条数正好=要求数、末列满 20 吗？**　10. 无虚假证言/攻击真人/越界吗？
11. 本地落表使用了 `--check`，且只在退出码 0 后交付吗？　12. 落表前确认了工具真的可用（没谎报建表成功）吗？
